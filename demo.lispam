


(def foldl (lambda (f acc l)
  (if (nil? l) acc
    (foldl f (f (head l) acc) (tail l)))))

(println (foldl sum 0 (quote (1 2 3 4 5))))

(def msum (macro (x y) (+ x y)))
(def fsum (lambda (x y) (+ x y)))



